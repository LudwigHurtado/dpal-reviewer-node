node_modules
dist
.DS_Store
*.local
.env
.env.*
!.env.example
.vercel
.env*.local

# Nested clone of https://github.com/LudwigHurtado/dpal-ai-server — separate repo; push from that folder, not from this tree
dpal-ai-server/
